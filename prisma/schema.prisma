generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Lead {
  id                           String                 @id @default(cuid())
  customerName                 String
  companyName                  String?
  phone                        String
  email                        String?
  kakaoId                      String?
  industry                     String
  boxType                      String
  widthMm                      Int?
  depthMm                      Int?
  heightMm                     Int?
  quantityRange                String
  printOption                  String
  finishingOptions             String?
  desiredDeliveryDate          DateTime?
  budgetRange                  String?
  referenceNote                String?
  message                      String?
  packageType                  String?
  packageStructure             String?
  quantity                     String?
  sizeInfo                     String?
  hasPhysicalProduct           String?
  hasDesignFile                String?
  hasDieline                   String?
  desiredDueDate               String?
  isUrgent                     Boolean                @default(false)
  readinessChecklist           String?
  readinessScore               Int?
  consultationNotes            String?
  consultationSummaryTitle     String?
  consultationSummaryOverview  String?
  consultationPriorityNotes    String?
  consultationMissingItems     String?
  consultationRiskNotes        String?
  consultationNextActions      String?
  consultationSummaryUpdatedAt DateTime?
  privacyConsent               Boolean                @default(false)
  privacyConsentAt             DateTime?
  marketingConsent             Boolean                @default(false)
  marketingConsentAt           DateTime?
  utmSource                    String?
  utmMedium                    String?
  utmCampaign                  String?
  utmTerm                      String?
  utmContent                   String?
  referrer                     String?
  landingPath                  String?
  sourceCaseSlug               String?
  sourceCaseTitle              String?
  estimatedUnitPriceMinKrw     Int?
  estimatedUnitPriceMaxKrw     Int?
  estimatedTotalPriceMinKrw    Int?
  estimatedTotalPriceMaxKrw    Int?
  estimateLabel                String?
  estimateDisclaimer           String?
  quoteComplexityLevel         String?
  quoteConfidenceLevel         String?
  quoteCalculationNotes        String?
  quoteMissingFields           String?
  estimatedPriceRange          String?
  leadScore                    Int                    @default(0)
  status                       String                 @default("NEW")
  nextFollowUpAt               DateTime?
  lastContactedAt              DateTime?
  salesNote                    String?
  quotedAt                     DateTime?
  orderConfirmedAt             DateTime?
  closedAt                     DateTime?
  confirmedOrderAmountKrw      Int?
  lostReason                   String?
  adminMemo                    String?
  createdAt                    DateTime               @default(now())
  updatedAt                    DateTime               @updatedAt
  quoteProposals               QuoteProposal[]
  quoteActivityLogs            QuoteActivityLog[]
  communicationLogs            LeadCommunicationLog[]
  salesTasks                   SalesTask[]

  @@index([createdAt])
  @@index([leadScore])
  @@index([status])
  @@index([nextFollowUpAt])
  @@index([lastContactedAt])
  @@index([quotedAt])
  @@index([orderConfirmedAt])
  @@index([closedAt])
  @@index([utmSource])
  @@index([utmCampaign])
  @@index([sourceCaseSlug])
}

model QuoteRule {
  id                           String               @id @default(cuid())
  name                         String               @unique
  isActive                     Boolean              @default(true)
  boxType                      String
  quantityRange                String
  minQuantity                  Int?
  maxQuantity                  Int?
  baseUnitPriceMinKrw          Int
  baseUnitPriceMaxKrw          Int
  sizeSmallThreshold           Int                  @default(500000)
  sizeMediumThreshold          Int                  @default(2000000)
  sizeLargeThreshold           Int                  @default(6000000)
  smallSizeMultiplier          Float                @default(0.9)
  mediumSizeMultiplier         Float                @default(1)
  largeSizeMultiplier          Float                @default(1.25)
  extraLargeSizeMultiplier     Float                @default(1.55)
  printNoneMultiplier          Float                @default(1)
  printOneColorMultiplier      Float                @default(1.08)
  printFullColorMultiplier     Float                @default(1.18)
  printFoilEmbossMultiplier    Float                @default(1.3)
  finishingBaseAddMinKrw       Int                  @default(0)
  finishingBaseAddMaxKrw       Int                  @default(0)
  complexityLowMultiplier      Float                @default(0.95)
  complexityNormalMultiplier   Float                @default(1)
  complexityHighMultiplier     Float                @default(1.18)
  complexityVeryHighMultiplier Float                @default(1.35)
  minOrderPriceKrw             Int                  @default(0)
  notes                        String?
  createdAt                    DateTime             @default(now())
  updatedAt                    DateTime             @updatedAt
  changeLogs                   QuoteRuleChangeLog[]

  @@index([isActive])
  @@index([boxType])
  @@index([quantityRange])
}

model QuoteProposal {
  id                            String                          @id @default(cuid())
  leadId                        String?
  lead                          Lead?                           @relation(fields: [leadId], references: [id], onDelete: SetNull)
  proposalNumber                String                          @unique
  status                        String                          @default("DRAFT")
  revisionGroupId               String?
  revisionNumber                Int                             @default(1)
  parentProposalId              String?
  supersededByProposalId        String?
  isLatestRevision              Boolean                         @default(true)
  revisionReason                String?
  revisedFromCustomerResponseId String?
  supersededAt                  DateTime?
  title                         String
  customerNameSnapshot          String
  companyNameSnapshot           String?
  phoneSnapshot                 String?
  emailSnapshot                 String?
  kakaoIdSnapshot               String?
  boxType                       String
  industry                      String
  quantityLabel                 String?
  quantityCount                 Int?
  specificationSummary          String?
  productionNotes               String?
  deliveryEstimateText          String?
  paymentTerms                  String?
  validUntil                    DateTime?
  subtotalAmountKrw             Int                             @default(0)
  vatAmountKrw                  Int                             @default(0)
  totalAmountKrw                Int                             @default(0)
  vatIncluded                   Boolean                         @default(false)
  customerMessage               String?
  internalMemo                  String?
  basedOnEstimateLabel          String?
  basedOnUnitPriceMinKrw        Int?
  basedOnUnitPriceMaxKrw        Int?
  basedOnTotalPriceMinKrw       Int?
  basedOnTotalPriceMaxKrw       Int?
  estimateComparisonStatus      String?
  createdAt                     DateTime                        @default(now())
  updatedAt                     DateTime                        @updatedAt
  items                         QuoteProposalItem[]
  shareLinks                    QuoteProposalShareLink[]
  customerResponses             QuoteProposalCustomerResponse[]
  activityLogs                  QuoteActivityLog[]
  salesTasks                    SalesTask[]

  @@index([leadId])
  @@index([status])
  @@index([proposalNumber])
  @@index([revisionGroupId])
  @@index([revisionNumber])
  @@index([parentProposalId])
  @@index([supersededByProposalId])
  @@index([isLatestRevision])
  @@index([revisedFromCustomerResponseId])
  @@index([createdAt])
  @@index([validUntil])
  @@index([boxType])
}

model QuoteProposalItem {
  id              String        @id @default(cuid())
  quoteProposalId String
  quoteProposal   QuoteProposal @relation(fields: [quoteProposalId], references: [id], onDelete: Cascade)
  sortOrder       Int           @default(0)
  itemName        String
  description     String?
  quantity        Int
  unitPriceKrw    Int
  amountKrw       Int
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  @@index([quoteProposalId])
  @@index([sortOrder])
}

model QuoteProposalShareLink {
  id                String                          @id @default(cuid())
  quoteProposalId   String
  quoteProposal     QuoteProposal                   @relation(fields: [quoteProposalId], references: [id], onDelete: Cascade)
  tokenHash         String                          @unique
  tokenPreview      String
  status            String                          @default("ACTIVE")
  expiresAt         DateTime
  revokedAt         DateTime?
  firstViewedAt     DateTime?
  lastViewedAt      DateTime?
  viewCount         Int                             @default(0)
  createdBy         String?
  createdAt         DateTime                        @default(now())
  updatedAt         DateTime                        @updatedAt
  customerResponses QuoteProposalCustomerResponse[]

  @@index([quoteProposalId])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

model QuoteProposalCustomerResponse {
  id              String                 @id @default(cuid())
  quoteProposalId String
  quoteProposal   QuoteProposal          @relation(fields: [quoteProposalId], references: [id], onDelete: Cascade)
  shareLinkId     String
  shareLink       QuoteProposalShareLink @relation(fields: [shareLinkId], references: [id], onDelete: Cascade)
  responseType    String
  message         String?
  responderName   String?
  createdAt       DateTime               @default(now())

  @@unique([shareLinkId])
  @@index([quoteProposalId])
  @@index([responseType])
  @@index([createdAt])
}

model QuoteActivityLog {
  id              String         @id @default(cuid())
  leadId          String?
  lead            Lead?          @relation(fields: [leadId], references: [id], onDelete: SetNull)
  quoteProposalId String?
  quoteProposal   QuoteProposal? @relation(fields: [quoteProposalId], references: [id], onDelete: SetNull)
  type            String
  actor           String
  message         String
  metadataJson    String?
  createdAt       DateTime       @default(now())

  @@index([leadId])
  @@index([quoteProposalId])
  @@index([type])
  @@index([actor])
  @@index([createdAt])
}

model LeadCommunicationLog {
  id             String    @id @default(cuid())
  leadId         String
  lead           Lead      @relation(fields: [leadId], references: [id], onDelete: Cascade)
  channel        String
  direction      String
  summary        String
  detail         String?
  contactedAt    DateTime
  nextFollowUpAt DateTime?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([leadId])
  @@index([channel])
  @@index([direction])
  @@index([contactedAt])
  @@index([nextFollowUpAt])
}

model SalesTask {
  id              String         @id @default(cuid())
  leadId          String?
  lead            Lead?          @relation(fields: [leadId], references: [id], onDelete: SetNull)
  quoteProposalId String?
  quoteProposal   QuoteProposal? @relation(fields: [quoteProposalId], references: [id], onDelete: SetNull)
  title           String
  description     String?
  type            String         @default("GENERAL")
  priority        String         @default("NORMAL")
  status          String         @default("TODO")
  dueAt           DateTime?
  completedAt     DateTime?
  cancelledAt     DateTime?
  assignedTo      String?
  sourceType      String?
  sourceId        String?
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt

  @@index([leadId])
  @@index([quoteProposalId])
  @@index([type])
  @@index([priority])
  @@index([status])
  @@index([dueAt])
  @@index([sourceType])
  @@index([sourceId])
  @@index([createdAt])
}

model QuoteRuleChangeLog {
  id                    String     @id @default(cuid())
  quoteRuleId           String?
  quoteRule             QuoteRule? @relation(fields: [quoteRuleId], references: [id], onDelete: SetNull)
  quoteRuleNameSnapshot String
  changeType            String
  beforeJson            String?
  afterJson             String?
  changeReason          String?
  changedBy             String?
  createdAt             DateTime   @default(now())

  @@index([quoteRuleId])
  @@index([changeType])
  @@index([createdAt])
}

model PortfolioCase {
  id                      String    @id @default(cuid())
  title                   String
  slug                    String    @unique
  status                  String    @default("DRAFT")
  featured                Boolean   @default(false)
  sortOrder               Int       @default(0)
  industry                String
  boxType                 String
  packageStructure        String?
  casePurpose             String?
  productName             String?
  clientName              String?
  isClientNamePublic      Boolean   @default(false)
  quantityRange           String?
  widthMm                 Int?
  depthMm                 Int?
  heightMm                Int?
  paperType               String?
  boardThickness          String?
  printOption             String?
  finishingOptions        String?
  mainImageUrl            String?
  mainImageAlt            String?
  imageCaption            String?
  imageUrls               String?
  shortDescription        String
  projectOverview         String?
  productionPoint         String?
  specificationSummary    String?
  seoTitle                String?
  seoDescription          String?
  tags                    String?
  publicApprovalConfirmed Boolean   @default(false)
  publicApprovalMemo      String?
  publicApprovedAt        DateTime?
  publicApprovalBy        String?
  publishedAt             DateTime?
  createdAt               DateTime  @default(now())
  updatedAt               DateTime  @updatedAt

  @@index([status])
  @@index([publicApprovalConfirmed])
  @@index([featured])
  @@index([sortOrder])
  @@index([publishedAt])
  @@index([industry])
  @@index([boxType])
  @@index([packageStructure])
  @@index([casePurpose])
}

model MarketingCost {
  id          String   @id @default(cuid())
  costDate    DateTime
  channel     String
  utmSource   String?
  utmMedium   String?
  utmCampaign String?
  amountKrw   Int
  memo        String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([costDate])
  @@index([channel])
  @@index([utmSource])
  @@index([utmCampaign])
}

model UploadProject {
  id                 String          @id @default(cuid())
  uploadCode         String?         @unique @map("upload_code")
  cafe24OrderNumber String          @map("cafe24_order_number")
  cafe24MallId       String?         @map("cafe24_mall_id")
  cafe24OrderId      String?         @map("cafe24_order_id")
  cafe24OrderNo      String?         @map("cafe24_order_no")
  cafe24MemberId     String?         @map("cafe24_member_id")
  cafe24OrderMemo    String?         @map("cafe24_order_memo")
  companyName        String?         @map("company_name")
  contactName        String?         @map("contact_name")
  customerName       String          @map("customer_name")
  phone              String
  email              String?
  kakaoId            String?         @map("kakao_id")
  contactMethod      String?         @map("contact_method")
  productName        String          @map("product_name")
  productOptionText  String?         @map("product_option_text")
  requestMemo        String?         @map("request_memo")
  privacyAgreed      Boolean         @default(false) @map("privacy_agreed")
  status             String          @default("upload_waiting")
  reviewStatus       String          @default("upload_waiting") @map("review_status")
  adminMemo          String?         @map("admin_memo")
  linkedAt           DateTime?       @map("linked_at")
  linkSource         String?         @map("link_source")
  orderSyncedAt      DateTime?       @map("order_synced_at")
  createdAt          DateTime        @default(now()) @map("created_at")
  updatedAt          DateTime        @updatedAt @map("updated_at")
  files              UploadedFile[]
  reviewLogs         FileReviewLog[]
  cafe24WebhookEvents Cafe24WebhookEvent[]

  @@index([uploadCode])
  @@index([cafe24OrderNumber])
  @@index([cafe24MallId])
  @@index([cafe24OrderId])
  @@index([cafe24OrderNo])
  @@index([companyName])
  @@index([contactName])
  @@index([customerName])
  @@index([productName])
  @@index([status])
  @@index([reviewStatus])
  @@index([createdAt])
  @@map("upload_projects")
}

model Cafe24Token {
  id           String   @id @default(cuid())
  mallId       String   @unique @map("mall_id")
  accessToken  String   @map("access_token")
  refreshToken String   @map("refresh_token")
  expiresAt    DateTime @map("expires_at")
  scopes       String?
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  @@index([expiresAt])
  @@map("cafe24_tokens")
}

model Cafe24WebhookEvent {
  id              String         @id @default(cuid())
  eventType       String?        @map("event_type")
  eventId         String?        @map("event_id")
  mallId          String?        @map("mall_id")
  orderId         String?        @map("order_id")
  orderNo         String?        @map("order_no")
  uploadCode      String?        @map("upload_code")
  payloadJson     Json           @map("payload_json")
  processedAt     DateTime?      @map("processed_at")
  status          String         @default("RECEIVED")
  errorMessage    String?        @map("error_message")
  uploadProjectId String?        @map("upload_project_id")
  uploadProject   UploadProject? @relation(fields: [uploadProjectId], references: [id], onDelete: SetNull)
  createdAt       DateTime       @default(now()) @map("created_at")

  @@unique([mallId, eventId])
  @@index([eventType])
  @@index([mallId])
  @@index([orderId])
  @@index([orderNo])
  @@index([uploadCode])
  @@index([status])
  @@index([createdAt])
  @@map("cafe24_webhook_events")
}

model UploadedFile {
  id               String          @id @default(cuid())
  projectId        String          @map("project_id")
  project          UploadProject   @relation(fields: [projectId], references: [id], onDelete: Cascade)
  originalFilename String          @map("original_filename")
  storedFilename   String          @map("stored_filename")
  storageBucket    String          @map("storage_bucket")
  storageKey       String          @map("storage_key")
  fileSize         Int             @map("file_size")
  fileType         String?         @map("file_type")
  fileExtension    String          @map("file_extension")
  version          Int             @default(1)
  uploadStatus     String          @default("prepared") @map("upload_status")
  reviewStatus     String          @default("upload_waiting") @map("review_status")
  uploadedAt       DateTime?       @map("uploaded_at")
  createdAt        DateTime        @default(now()) @map("created_at")
  updatedAt        DateTime        @updatedAt @map("updated_at")
  reviewLogs       FileReviewLog[]

  @@index([projectId])
  @@index([projectId, version])
  @@index([storageKey])
  @@index([uploadStatus])
  @@index([reviewStatus])
  @@index([uploadedAt])
  @@map("uploaded_files")
}

model FileReviewLog {
  id        String        @id @default(cuid())
  projectId String        @map("project_id")
  project   UploadProject @relation(fields: [projectId], references: [id], onDelete: Cascade)
  fileId    String?       @map("file_id")
  file      UploadedFile? @relation(fields: [fileId], references: [id], onDelete: SetNull)
  status    String
  message   String?
  createdBy String?       @map("created_by")
  createdAt DateTime      @default(now()) @map("created_at")

  @@index([projectId])
  @@index([fileId])
  @@index([status])
  @@index([createdAt])
  @@map("file_review_logs")
}
